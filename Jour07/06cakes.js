const cakes = [
   {
       name : "Cake",
       flavor: "vanilla",
       status : "available"
       {
           name : "brownie"
           flavor: "chocalate",
           status: "available".
   },]
   const chocalateCakes =cakes.filter(function(Cake){
       if (cake.flavor==="chocolate"){
           return cake;
       }
       {).map (function(chocalite){
           return{name : chocolateCake.name}
           flavor: chocolateCake.flavor,
           status:"sold out"}.
    };
    console.log(chocalateCakes);
